# T5 · 首页

> 状态：🔲 待做 ｜ 依赖：T2/3/4 ｜ 目录：web/src/

## 范围

- HomePage：Hero / MonthRail / MonthRows·PlanCard / Archive / SafetyBanner / 成就抽屉（Drawer / BadgeWall / StatsBar / FootprintMap）。
- `hooks/useLocalStorage`：沿用现有 4 个 key 与格式（`gear:<id>:<idx>`、`tasks:<id>:<idx>`、`badges:unlocked`、`plans:done`），本地既有数据不丢。
- milestone 求值（firstType / count / minKm）暂留前端。
- 抽屉开关、`?panel=badges|footprint` 自动打开 等交互保留。

## 产出

- HomePage 及其子组件、hooks/useLocalStorage

## 验收

- 首页月份/卡片/归档/安全横幅渲染正确；成就抽屉徽章墙/统计条/足迹图可用。
- localStorage 旧数据（本机已有勾选/打卡）不丢。

## 进度 / 完成说明

（完成时记录）

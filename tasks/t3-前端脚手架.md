# T3 · 前端脚手架 + API 客户端

> 状态：🔲 待做 ｜ 依赖：T1 ｜ 目录：web/

## 范围

- `web/` Vite React-TS 模板 + 依赖（react-router-dom 等）。
- `vite.config.ts`：dev 代理 `/api` → `http://localhost:8000`；`App.tsx` 路由（`/`、`/plan/:id`）占位。
- 移植 `site/css/style.css` → `web/src/styles.css`（保留设计 token 与整套卡片视觉）。
- openapi-typescript 从 `/openapi.json` 生成 `web/src/api/types.ts`；`client.ts` 封装 fetch。

## 产出

- `web/` 完整脚手架（package.json、vite.config.ts、App.tsx、styles.css、api/types.ts、api/client.ts）

## 验收

- `npm run dev` 起服务，`/` 与 `/plan/:id` 路由可达。
- `types.ts` 与后端 OpenAPI 一致；client.ts 能拉到 `/api/plans`。

## 进度 / 完成说明

（完成时记录）

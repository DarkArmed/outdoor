# T4 · 测试对齐与收尾

> 状态：🔲 待做 ｜ 依赖：T3

## 范围

- **状态迁移**：浏览器 `localStorage` 中的 gear/task/badges/plans:done 数据迁移到后端（首次登录/启动时同步）。
- **测试**：Vitest 覆盖公共组件、首页/详情页关键渲染、API client、认证状态。
- **新旧对比**：首页、详情页、地图、装备、徽章、足迹、打卡等行为与旧 `site/` 一致。
- **退役 `site/`**：删除旧静态站文件和相关同步脚本（确认新站功能完全对齐后）。
- **文档更新**：
  - `docs/tech/网站.md` 更新为 React 前端架构。
  - `docs/tech/操作手册.md` 更新为前后端 dev 运行方式。
  - `README.md` 更新为最终项目结构。
- **Git 收尾**：feature worktree 内提交 → push → merge PR → 删除 worktree/分支。

## 验收

- `npx vitest` 通过。
- 新旧站并排对比无功能差异。
- `site/` 删除后不影响项目运行。
- 文档与实现一致，PR 合入 master。

## 完成说明

（待实现）

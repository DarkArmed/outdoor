# T3/T4 前端验收记录

日期：2026-09-14。基线：T2 合并提交 `9d669c4`；在 `codex/web-completion` 独立 worktree 完成。复用原 `feat/web-frontend` 的页面结构和成就计算思路，重写清单持久化、长按计时、出行选择与快照、画像编辑及迁移流程；原 worktree 未修改。

## 对齐结果

| 功能 | 验证 |
|---|---|
| 首页月份分组、备用方案、安全口诀 | 新旧首页截图核对；组件浏览器测试 |
| 详情目标、任务、行程、装备、安全、回顾 | 新旧页面内容核对；页面单元测试 |
| 多日行程 | 日标题与各日内容单元测试；两日露营浏览器流程 |
| 插图、自驾/徒步/足迹地图 | 旧渲染器与新函数完整 SVG 字符串对比；真实 API 截图检查 |
| 装备与任务 | 单项写入后端，刷新后仍勾选；旧数据导入后状态一致 |
| 打卡 | 提前释放与卸载取消；持续长按完成；失败可重试；普通/满星两条流程 |
| 徽章与足迹 | 打卡事务解锁，重复请求不重复打卡；抽屉 URL、键盘、焦点与截图 |
| 用户与出行 | 注册、画像保存刷新、同浏览器切换账号隔离、指定出行快照、改名、删除 |
| 旧记录迁移 | 实际 gear/tasks 键解析；重复导入不重复创建、不覆盖新状态；错误整批拒绝 |
| 静态站退役 | 公共数据内容与 Git 基线逐文件一致；新数据库迁移与 seed 成功；删除 site 后重新通过浏览器测试 |

新版保留旧站卡通插图、纸色地图和核心内容。新增账号导航与出行管理；清单改为后端确认后显示保存结果，布局采用 React 公共组件，非整页像素完全一致。SVG 渲染本身保持旧算法输出。

## 最终检查

- `npm run build`：通过。
- `npm run lint`：通过；保留 T2 已有 AuthContext Fast Refresh 提示，无新增 lint 警告。
- `npm run test -- --run`：37 项通过。
- `python -m pytest -q -p no:cacheprovider`：30 项通过；依赖弃用提示不影响结果。
- `npm run test:e2e -- --workers=2`：12 项通过，Desktop Chrome 和 Pixel 5。
- 新 SQLite：Alembic `upgrade head → downgrade base → upgrade head` 与完整 seed 通过。
- 浏览器测试使用临时 SQLite、8015/5175 独立端口；截图和报告为本地测试产物，不提交账号信息。

## 合并后的操作

先运行 `alembic upgrade head`，再启动 API 与前端。原 file:// 旧站状态需在原浏览器导出后从画像页导入，见操作手册。已有公共表 seed 仍只插入缺失记录；画像变化不自动重新规划公共路线。

PR 由用户合并；合并后再清理当前开发 worktree。
